using System.CommandLine;
using Microsoft.Extensions.DependencyInjection;

namespace KopioRapido.CLI.Commands;

public class MoveCommand : BaseCommand
{
    public static Command Create(IServiceProvider services)
    {
        var cmd = new Command("move", "Move files from source to destination (copy then delete source)");
        // TODO: Add full implementation similar to CopyCommand
        return cmd;
    }

    private MoveCommand(IServiceProvider services) : base(services) { }
}
