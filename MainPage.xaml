<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:KopioRapido.ViewModels"
             xmlns:models="clr-namespace:KopioRapido.Models"
             x:Class="KopioRapido.MainPage"
             Title="KopioRapido - High-Speed File Copy"
             x:DataType="viewmodels:MainViewModel">

    <Grid RowDefinitions="Auto,*,Auto,Auto,Auto" Padding="20">

        <!-- Title -->
        <Label Grid.Row="0"
               Text="KopioRapido"
               FontSize="32"
               FontAttributes="Bold"
               HorizontalOptions="Center"
               Margin="0,0,0,20"/>

        <!-- Dual Pane for Source and Destination -->
        <Grid Grid.Row="1" ColumnDefinitions="*,20,*" Margin="0,0,0,20">

            <!-- Source Pane -->
            <Border x:Name="SourceBorder"
                    Grid.Column="0"
                    BackgroundColor="{AppThemeBinding Light=#FFFFFF, Dark=#1E1E1E}"
                    Stroke="{AppThemeBinding Light=#0078D4, Dark=#60A5FA}"
                    StrokeThickness="2"
                    Padding="0">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="8"/>
                </Border.StrokeShape>
                <!-- Drag-drop handled by native Windows implementation in code-behind -->

                <Grid RowDefinitions="Auto,*,Auto">
                    <!-- Header -->
                    <Grid Grid.Row="0"
                          BackgroundColor="{AppThemeBinding Light=#0078D4, Dark=#1E3A8A}"
                          Padding="15,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Label Grid.Column="0"
                               Text="SOURCE FILES"
                               FontSize="14"
                               FontAttributes="Bold"
                               TextColor="White"/>
                        <Label Grid.Column="1"
                               Text="{Binding SourceFiles.Count, StringFormat='{0} files'}"
                               FontSize="12"
                               TextColor="White"
                               VerticalOptions="Center"/>
                    </Grid>

                    <!-- File List or Empty State -->
                    <Grid Grid.Row="1">
                        <!-- Empty State -->
                        <VerticalStackLayout IsVisible="{Binding SourceFiles.Count, Converter={StaticResource IsStringNotNullOrEmptyConverter}, ConverterParameter=inverse}"
                                           VerticalOptions="Center"
                                           HorizontalOptions="Center"
                                           Padding="20">
                            <Label Text="📁"
                                   FontSize="48"
                                   HorizontalOptions="Center"
                                   Margin="0,0,0,10"/>
                            <Label Text="Drag &amp; drop a folder here"
                                   FontSize="16"
                                   HorizontalOptions="Center"
                                   TextColor="{AppThemeBinding Light=#666666, Dark=#999999}"/>
                            <Label Text="or"
                                   FontSize="14"
                                   HorizontalOptions="Center"
                                   Margin="0,5,0,5"
                                   TextColor="{AppThemeBinding Light=#888888, Dark=#AAAAAA}"/>
                            <Button Text="Select Source Folder"
                                    Command="{Binding SelectSourceCommand}"
                                    HorizontalOptions="Center"
                                    BackgroundColor="{AppThemeBinding Light=#0078D4, Dark=#60A5FA}"
                                    TextColor="White"/>
                        </VerticalStackLayout>

                        <!-- File List -->
                        <ScrollView>
                            <CollectionView ItemsSource="{Binding SourceFiles}">
                                <CollectionView.ItemTemplate>
                                    <DataTemplate x:DataType="models:FileItem">
                                        <Border BackgroundColor="{AppThemeBinding Light=#F9F9F9, Dark=#2A2A2A}"
                                                Margin="5,2"
                                                Padding="10,8">
                                            <Border.StrokeShape>
                                                <RoundRectangle CornerRadius="4"/>
                                            </Border.StrokeShape>
                                            <Grid ColumnDefinitions="*,Auto,80" RowDefinitions="Auto,Auto">
                                                <!-- File Name -->
                                                <Label Grid.Column="0" Grid.Row="0"
                                                       Text="{Binding FileName}"
                                                       FontSize="13"
                                                       LineBreakMode="TailTruncation"
                                                       VerticalOptions="Center"/>

                                                <!-- File Size -->
                                                <Label Grid.Column="1" Grid.Row="0"
                                                       Text="{Binding FormattedSize}"
                                                       FontSize="11"
                                                       TextColor="{AppThemeBinding Light=#666666, Dark=#999999}"
                                                       VerticalOptions="Center"
                                                       Margin="10,0"/>

                                                <!-- Progress Percentage or Retry Status -->
                                                <Label Grid.Column="2" Grid.Row="0"
                                                       Text="{Binding Progress, StringFormat='{0:F0}%'}"
                                                       FontSize="11"
                                                       TextColor="{AppThemeBinding Light=#0078D4, Dark=#60A5FA}"
                                                       HorizontalOptions="End"
                                                       VerticalOptions="Center"
                                                       IsVisible="{Binding IsCopying}"/>

                                                <Label Grid.Column="2" Grid.Row="0"
                                                       Text="{Binding RetryAttempt, StringFormat='Retry {0}...'}"
                                                       FontSize="11"
                                                       TextColor="{AppThemeBinding Light=#F59E0B, Dark=#FBBF24}"
                                                       HorizontalOptions="End"
                                                       VerticalOptions="Center"
                                                       FontAttributes="Bold"
                                                       IsVisible="{Binding IsRetrying}"/>

                                                <!-- Progress Bar -->
                                                <ProgressBar Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="1"
                                                           Progress="{Binding Progress, Converter={StaticResource PercentToProgressConverter}}"
                                                           HeightRequest="4"
                                                           Margin="0,4,0,0"
                                                           IsVisible="{Binding IsCopying}"
                                                           ProgressColor="{AppThemeBinding Light=#0078D4, Dark=#60A5FA}"/>

                                                <!-- Retry Progress Bar (Indeterminate) -->
                                                <Border Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="1"
                                                        HeightRequest="4"
                                                        Margin="0,4,0,0"
                                                        IsVisible="{Binding IsRetrying}"
                                                        BackgroundColor="{AppThemeBinding Light=#FEF3C7, Dark=#78350F}">
                                                    <Border.StrokeShape>
                                                        <RoundRectangle CornerRadius="2"/>
                                                    </Border.StrokeShape>
                                                    <Label Text="⏳ Retrying..."
                                                           FontSize="9"
                                                           TextColor="{AppThemeBinding Light=#F59E0B, Dark=#FBBF24}"
                                                           HorizontalOptions="Center"
                                                           VerticalOptions="Center"/>
                                                </Border>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>
                        </ScrollView>
                    </Grid>

                    <!-- Source Path Footer -->
                    <Border Grid.Row="2"
                            BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#252525}"
                            Padding="10,8">
                        <Grid ColumnDefinitions="*,Auto">
                            <Label Grid.Column="0"
                                   Text="{Binding SourcePath, StringFormat='📂 {0}'}"
                                   FontSize="11"
                                   LineBreakMode="TailTruncation"
                                   VerticalOptions="Center"
                                   TextColor="{AppThemeBinding Light=#666666, Dark=#999999}"
                                   IsVisible="{Binding SourcePath, Converter={StaticResource IsStringNotNullOrEmptyConverter}}"/>
                            <Button Grid.Column="1"
                                    Text="📁 Browse..."
                                    Command="{Binding SelectSourceCommand}"
                                    FontSize="12"
                                    Padding="12,6"
                                    BackgroundColor="{AppThemeBinding Light=#0078D4, Dark=#1E3A8A}"
                                    TextColor="White"
                                    CornerRadius="4"/>
                        </Grid>
                    </Border>
                </Grid>
            </Border>

            <!-- Separator -->
            <BoxView Grid.Column="1"
                     WidthRequest="20"
                     Color="Transparent"/>

            <!-- Destination Pane -->
            <Border x:Name="DestinationBorder"
                    Grid.Column="2"
                    BackgroundColor="{AppThemeBinding Light=#FFFFFF, Dark=#1E1E1E}"
                    Stroke="{AppThemeBinding Light=#10B981, Dark=#34D399}"
                    StrokeThickness="2"
                    Padding="0">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="8"/>
                </Border.StrokeShape>
                <!-- Drag-drop handled by native Windows implementation in code-behind -->

                <Grid RowDefinitions="Auto,*,Auto">
                    <!-- Header -->
                    <Grid Grid.Row="0"
                          BackgroundColor="{AppThemeBinding Light=#10B981, Dark=#065F46}"
                          Padding="15,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Label Grid.Column="0"
                               Text="COPIED FILES"
                               FontSize="14"
                               FontAttributes="Bold"
                               TextColor="White"/>
                        <Label Grid.Column="1"
                               Text="{Binding DestinationFiles.Count, StringFormat='{0} copied'}"
                               FontSize="12"
                               TextColor="White"
                               VerticalOptions="Center"/>
                    </Grid>

                    <!-- File List or Empty State -->
                    <Grid Grid.Row="1">
                        <!-- Empty State -->
                        <VerticalStackLayout IsVisible="{Binding DestinationFiles.Count, Converter={StaticResource IsStringNotNullOrEmptyConverter}, ConverterParameter=inverse}"
                                           VerticalOptions="Center"
                                           HorizontalOptions="Center"
                                           Padding="20">
                            <Label Text="📋"
                                   FontSize="48"
                                   HorizontalOptions="Center"
                                   Margin="0,0,0,10"/>
                            <Label Text="Select destination folder"
                                   FontSize="16"
                                   HorizontalOptions="Center"
                                   TextColor="{AppThemeBinding Light=#666666, Dark=#999999}"/>
                            <Label Text="or"
                                   FontSize="14"
                                   HorizontalOptions="Center"
                                   Margin="0,5,0,5"
                                   TextColor="{AppThemeBinding Light=#888888, Dark=#AAAAAA}"/>
                            <Button Text="Select Destination Folder"
                                    Command="{Binding SelectDestinationCommand}"
                                    HorizontalOptions="Center"
                                    BackgroundColor="{AppThemeBinding Light=#10B981, Dark=#34D399}"
                                    TextColor="White"/>
                        </VerticalStackLayout>

                        <!-- File List -->
                        <ScrollView>
                            <CollectionView ItemsSource="{Binding DestinationFiles}">
                                <CollectionView.ItemTemplate>
                                    <DataTemplate x:DataType="models:FileItem">
                                        <Border BackgroundColor="{AppThemeBinding Light=#F0FDF4, Dark=#064E3B}"
                                                Margin="5,2"
                                                Padding="10,8">
                                            <Border.StrokeShape>
                                                <RoundRectangle CornerRadius="4"/>
                                            </Border.StrokeShape>
                                            <Grid ColumnDefinitions="*,Auto,Auto">
                                                <Label Grid.Column="0"
                                                       Text="{Binding FileName}"
                                                       FontSize="13"
                                                       LineBreakMode="TailTruncation"
                                                       VerticalOptions="Center"/>
                                                <Label Grid.Column="1"
                                                       Text="{Binding FormattedSize}"
                                                       FontSize="11"
                                                       TextColor="{AppThemeBinding Light=#666666, Dark=#999999}"
                                                       VerticalOptions="Center"
                                                       Margin="10,0"/>
                                                <Label Grid.Column="2"
                                                       Text="✓"
                                                       FontSize="16"
                                                       TextColor="{AppThemeBinding Light=#10B981, Dark=#34D399}"
                                                       VerticalOptions="Center"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>
                        </ScrollView>
                    </Grid>

                    <!-- Destination Path Footer -->
                    <Border Grid.Row="2"
                            BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#252525}"
                            Padding="10,8">
                        <Grid ColumnDefinitions="*,Auto">
                            <Label Grid.Column="0"
                                   Text="{Binding DestinationPath, StringFormat='📂 {0}'}"
                                   FontSize="11"
                                   LineBreakMode="TailTruncation"
                                   VerticalOptions="Center"
                                   TextColor="{AppThemeBinding Light=#666666, Dark=#999999}"
                                   IsVisible="{Binding DestinationPath, Converter={StaticResource IsStringNotNullOrEmptyConverter}}"/>
                            <Button Grid.Column="1"
                                    Text="📁 Browse..."
                                    Command="{Binding SelectDestinationCommand}"
                                    FontSize="12"
                                    Padding="12,6"
                                    BackgroundColor="{AppThemeBinding Light=#10B981, Dark=#065F46}"
                                    TextColor="White"
                                    CornerRadius="4"/>
                        </Grid>
                    </Border>
                </Grid>
            </Border>
        </Grid>

        <!-- Progress Section (Only visible when copying) -->
        <VerticalStackLayout Grid.Row="2" Spacing="10" Margin="0,0,0,20" IsVisible="{Binding IsCopying}">

            <!-- Overall Progress -->
            <Border BackgroundColor="{AppThemeBinding Light=#F8F8F8, Dark=#252525}"
                    Stroke="{AppThemeBinding Light=#DDDDDD, Dark=#444444}"
                    StrokeThickness="1"
                    Padding="15">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="6"/>
                </Border.StrokeShape>
                <Grid RowDefinitions="Auto,Auto,Auto,Auto" RowSpacing="8">
                    <Label Grid.Row="0"
                           Text="Overall Progress"
                           FontSize="14"
                           FontAttributes="Bold"/>

                    <ProgressBar Grid.Row="1"
                                 Progress="{Binding OverallProgress, Converter={StaticResource PercentToProgressConverter}}"
                                 HeightRequest="12"
                                 ProgressColor="{AppThemeBinding Light=#0078D4, Dark=#60A5FA}"/>

                    <Label Grid.Row="2"
                           Text="{Binding OverallProgress, StringFormat='{0:F1}%'}"
                           HorizontalOptions="Center"
                           FontSize="16"
                           FontAttributes="Bold"
                           TextColor="{AppThemeBinding Light=#0078D4, Dark=#60A5FA}"/>

                    <!-- Speed and Stats -->
                    <Grid Grid.Row="3" ColumnDefinitions="*,*,*">
                        <Label Grid.Column="0"
                               Text="{Binding CurrentSpeed, StringFormat='Speed: {0}'}"
                               FontSize="11"
                               TextColor="{AppThemeBinding Light=#666666, Dark=#999999}"
                               HorizontalOptions="Start"/>

                        <Label Grid.Column="1"
                               Text="{Binding AverageSpeed, StringFormat='Avg: {0}'}"
                               FontSize="11"
                               TextColor="{AppThemeBinding Light=#666666, Dark=#999999}"
                               HorizontalOptions="Center"/>

                        <Label Grid.Column="2"
                               Text="{Binding EstimatedTimeRemaining, StringFormat='ETA: {0}'}"
                               FontSize="11"
                               TextColor="{AppThemeBinding Light=#666666, Dark=#999999}"
                               HorizontalOptions="End"/>
                    </Grid>
                </Grid>
            </Border>
        </VerticalStackLayout>

        <!-- Control Buttons (Only visible when has files to copy and destination selected) -->
        <Grid Grid.Row="3" ColumnDefinitions="*,20,*" ColumnSpacing="0" Margin="0,0,0,20"
              IsVisible="{Binding HasFilesToCopy}">
            <Button Grid.Column="0"
                    Text="Start Copy"
                    Command="{Binding StartCopyCommand}"
                    BackgroundColor="{AppThemeBinding Light=#10B981, Dark=#059669}"
                    TextColor="White"
                    FontAttributes="Bold"
                    FontSize="16"
                    HeightRequest="50"
                    CornerRadius="8"
                    IsEnabled="{Binding IsCopying, Converter={StaticResource InverseBoolConverter}}"/>

            <Button Grid.Column="2"
                    Text="Cancel"
                    Command="{Binding CancelCopyCommand}"
                    BackgroundColor="{AppThemeBinding Light=#EF4444, Dark=#DC2626}"
                    TextColor="White"
                    FontAttributes="Bold"
                    FontSize="16"
                    HeightRequest="50"
                    CornerRadius="8"
                    IsVisible="{Binding IsCopying}"/>
        </Grid>

        <!-- Debug Log (temporary) -->
        <Border Grid.Row="4"
                BackgroundColor="{AppThemeBinding Light=#FAFAFA, Dark=#1E1E1E}"
                Stroke="{AppThemeBinding Light=#CCCCCC, Dark=#444444}"
                StrokeThickness="1"
                HeightRequest="100"
                Margin="0,10,0,0"
                Padding="10">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="4"/>
            </Border.StrokeShape>
            <ScrollView>
                <VerticalStackLayout>
                    <Label Text="Debug Log:" FontSize="10" FontAttributes="Bold" Margin="0,0,0,5"/>
                    <CollectionView ItemsSource="{Binding LogMessages}">
                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="x:String">
                                <Label Text="{Binding}"
                                       FontSize="9"
                                       FontFamily="Consolas,monospace"
                                       Padding="2"/>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </VerticalStackLayout>
            </ScrollView>
        </Border>

    </Grid>
</ContentPage>
